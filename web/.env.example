# recon0 daemon URL — panel buraya bağlanır.
# Bu dosyayı `.env.local` olarak kopyala ve kendi değerini gir.
# Değişiklikten sonra `npm run dev`'i yeniden başlat (NEXT_PUBLIC_* build-time inline).
NEXT_PUBLIC_RECON0_URL=http://localhost:8484
