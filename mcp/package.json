{
  "name": "recon0-mcp",
  "version": "1.0.0",
  "description": "MCP server for recon0 — exposes recon pipeline tools to Claude",
  "type": "module",
  "bin": {
    "recon0-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "node --import tsx/esm src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
